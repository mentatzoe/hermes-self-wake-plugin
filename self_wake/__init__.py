"""Hermes self-wake plugin: formal operator tools for session wake routing."""

__version__ = "1.0.0"


def register(ctx):
    """Register plugin with Hermes. Called once at startup."""
    # TODO: register tools, hooks, commands, and bundled skills
    # Example:
    # from self_wake.tools import (
    #     sessions_handler, subscribe_handler, receipts_handler, doctor_handler
    # )
    # ctx.register_tool("self_wake_sessions", sessions_handler, toolset="self_wake", ...)
    # ctx.register_tool("self_wake_subscribe_kanban", subscribe_handler, toolset="self_wake", ...)
    # ctx.register_tool("self_wake_receipts", receipts_handler, toolset="self_wake", ...)
    # ctx.register_tool("self_wake_doctor", doctor_handler, toolset="self_wake", ...)
    # ctx.register_hook("pre_llm_call", record_recent_session)
    # ctx.register_command("/self-wake", self_wake_command)
    # ctx.register_skill("kanban-self-wake")
    pass
